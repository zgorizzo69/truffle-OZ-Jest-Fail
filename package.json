{
  "name": "TEST",
  "version": "2.0.0",
  "description": "TEST",
  "scripts": {
    "test": "truffle compile && jest --coverage false --no-cache --verbose"
  },
  "devDependencies": {
    "@openzeppelin/test-environment": "0.1.3",
    "jest": "24.9.0",
    "truffle": "5.1.43"
  }
}
